import { IndexedTree } from "./tree";

import * as mocha from "mocha";
import * as chai from "chai";

const expect = chai.expect;
describe('Some tree tests', () => {
    it('should work', () => {
        expect(5+4).to.equal(9);
    })
})